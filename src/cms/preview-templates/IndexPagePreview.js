import React from 'react'

const IndexPagePreview = ({ entry, getAsset, widgetsFor }) => {
  console.log(entry.getIn(['data', 'hero', 'header']))
  return <div>foobar</div>
}


export default IndexPagePreview
